[package]
name = "claudcompress"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4", features = ["derive"] }
rustc-hash = "2"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
